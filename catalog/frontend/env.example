# ==================== CATALOG BACKEND API ====================
# Catalog service backend API URL
# For local development: http://localhost:3001/api
# For Docker: http://catalog_backend:3001/api (internal) or http://localhost:3001/api (external)
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# ==================== BOT SERVICE INTEGRATION ====================
# Bot service API URL for chatbot widget
# For local development: http://localhost:8386
# For Docker: http://bot_backend:8386 (internal) or http://localhost:8386 (external)
NEXT_PUBLIC_BOT_API_URL=http://localhost:8386

# Site ID for bot service (used to lookup tenant in bot database)
# Currently matches tenant.name in bot database (temporary solution)
# In production, this will map to tenant_id via a mapping table
# 
# Example: If tenant name is "catalog-001" in bot DB, use "catalog-001" here
# Default: catalog-001
NEXT_PUBLIC_BOT_SITE_ID=n8n-market
