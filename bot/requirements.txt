# Core Dependencies
pydantic>=2.0.0
python-dotenv>=1.0.0

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Async Support
aiohttp>=3.9.0
asyncio>=3.4.3

# Database
sqlalchemy[asyncio]>=2.0.23
asyncpg>=0.29.0
psycopg2-binary>=2.9.9  # Sync driver for Alembic migrations
alembic>=1.12.0

# Redis (Session Storage & Caching)
aioredis>=2.0.1
redis>=5.0.0

# Data Processing
pyyaml>=6.0.1
python-dateutil>=2.8.2

# AI/ML Models
# sentence-transformers>=2.2.2
# torch>=2.0.0
openai>=1.3.0
# anthropic>=0.7.0
# Note: LiteLLM is accessed via HTTP API, not Python client

# Authentication & Security
PyJWT>=2.8.0  # JWT token validation (shared với Catalog service)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Logging & Monitoring
structlog>=23.2.0

# OpenTelemetry (Distributed Tracing)
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
opentelemetry-instrumentation-fastapi>=0.42b0
opentelemetry-exporter-otlp-proto-grpc>=1.21.0
opentelemetry-exporter-otlp-proto-http>=1.21.0
# Note: ConsoleSpanExporter is included in opentelemetry-sdk, no separate package needed

# Vector Store (Qdrant client)
qdrant-client>=1.6.0

# HTTP Client
httpx>=0.25.0
tenacity>=8.2.3  # For retry logic

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
httpx>=0.25.0  # For testing async endpoints

# Code Quality
black>=23.11.0
flake8>=6.1.0
mypy>=1.7.0
isort>=5.12.0

# Type Hints
types-PyYAML>=6.0.12

cryptography>=41.0.0  # For RSA key conversion